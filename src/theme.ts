import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    text: {
      primary: "#efefef",
    },
    background: {
      default: "#111827",
    },
    primary: {
      main: "#6c4cec",
    },
  },
});

export default theme;
